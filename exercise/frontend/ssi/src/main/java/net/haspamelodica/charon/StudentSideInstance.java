package net.haspamelodica.charon;

import net.haspamelodica.charon.annotations.StudentSideInstanceMethodKind;

/**
 * Each subinterface of {@link StudentSideInstance} represents all instance operations on one class to be written by the student.
 * This includes calling instance methods, reading instance fields, and writing instance fields.
 * The operations are specified by annotating each method of a subinterface with {@link StudentSideInstanceMethodKind}.
 */
public interface StudentSideInstance
{}
